from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='predict the likelihood of a wildfire in the US ',
    author='wildfire-likelihood',
    license='MIT',
)
